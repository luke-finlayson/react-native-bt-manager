# Example Bluetooth Manager App

Mock example of a React Native app with a global Bluetooth connection manager.

Connection manager is provided by the library found in `./lib`.

### Usage

Run this example with

```sh
npm run ios
npm run android
```

